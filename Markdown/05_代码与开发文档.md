# 围栏代码块

```ts
function add(a: number, b: number): number {
  return a + b;
}
```

````md
```ts
function add(a: number, b: number): number {
  return a + b;
}
```
````

## 常用语言标识

```bash
npm run test
```

```json
{
  "name": "demo",
  "private": true
}
```

```yaml
services:
  app:
    image: example/app:latest
```

```sql
SELECT id, name
FROM users
WHERE active = true;
```

````md
```bash
npm run test
```

```json
{
  "name": "demo",
  "private": true
}
```

```yaml
services:
  app:
    image: example/app:latest
```

```sql
SELECT id, name
FROM users
WHERE active = true;
```
````

## 终端会话

```console
$ npm run lint
> demo@1.0.0 lint
> eslint .
```

````md
```console
$ npm run lint
> demo@1.0.0 lint
> eslint .
```
````

## 配置文件与敏感信息

```dotenv
PORT=3000
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/app
```

````md
```dotenv
PORT=3000
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/app
```
````

> [!danger] 不要提交真实密钥
> 示例中的 `USER`、`PASSWORD` 和 `HOST` 是占位符。文档中只给出变量名、脱敏值或示例值，真实令牌、私钥、连接串不得写入 Markdown、更不得推送到仓库。

## Diff 代码块

```diff
- const port = 8080;
+ const port = Number(process.env.PORT ?? 3000);
```

````md
```diff
- const port = 8080;
+ const port = Number(process.env.PORT ?? 3000);
```
````

## 不指定语言

```
原样展示，不进行语法高亮。
```

````md
```
原样展示，不进行语法高亮。
```
````

## 缩进代码块

    旧式代码块每行缩进四个空格。

```md
    旧式代码块每行缩进四个空格。
```

## 命令说明

每个可执行命令应在上下文中说明运行目录、前置条件、预期结果和风险。危险命令不要只给出一行可复制文本。

````md
在项目根目录执行以下命令。它会删除未跟踪文件，请先确认工作区状态：

```bash
git clean -nd
```
````
